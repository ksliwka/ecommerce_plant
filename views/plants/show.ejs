<% layout('layouts/boilerplate')%>
<div class="container">
  <div class="row mt-4">
    <div class="col-lg-6">
      <div class="mb-3">
        <img src="<%= plant.image%>" class="img-fluid" alt="..." />
      </div>
    </div>
    <div class="col-lg-6 mt-5">
      <div class="d-flex mb-3">
        <div class="col-6">
          <h1 class="fw-bold text-uppercase"><%= plant.title%></h1>
        </div>
        <div class="col-6 d-flex justify-content-end pe-3">
          <h1 class="text-muted fs-2">$<%= plant.price%></h1>
        </div>
      </div>

      <p class="text-muted mt-4"><%= plant.description%></p>

      <p class="mt-3 text-muted">Location: <%= plant.location%></p>
      <p class="">
        Submitted by:<a
          href="/user/<%= plant.author._id %> "
          class="text-dark ms-3"
        >
          <%= plant.author.username%>
        </a>
      </p>

      <div class="d-flex justify-content-between gap-4 mt-5">
        <div class="col-6">
          <% if(currentUser) {%>
          <form
            action="/user/<%= currentUser._id%>/cart/<%= plant._id %>  "
            method="POST"
          >
            <button class="btn btn-dark login_button w-50">ADD TO CART</button>
          </form>
          <% } %>
        </div>
        <div class="col-6">
          <button class="btn btn-outline-dark login_button w-50">
            ADD TO WHISHLIST
          </button>
        </div>
        <!-- <button
        class="btn btn-secondary"
        type="button"
        id="addBtn"
        data-type="plus"
      >
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-plus" viewBox="0 0 16 16">
        <path d="M8 4a.5.5 0 0 1 .5.5v3h3a.5.5 0 0 1 0 1h-3v3a.5.5 0 0 1-1 0v-3h-3a.5.5 0 0 1 0-1h3v-3A.5.5 0 0 1 8 4z"/>
      </svg>
      </button>
      <input
        type="text"
        class="form-control text-center"
        value="1"
        min="0"
        max="20"
        id="quantity"
      />
      <button
        class="btn btn-secondary"
        type="button"
        id="removeBtn"
        data-type="minus"
      >
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-file-minus" viewBox="0 0 16 16">
        <path d="M5.5 8a.5.5 0 0 1 .5-.5h4a.5.5 0 0 1 0 1H6a.5.5 0 0 1-.5-.5z"/>
        <path d="M4 0a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h8a2 2 0 0 0 2-2V2a2 2 0 0 0-2-2H4zm0 1h8a1 1 0 0 1 1 1v12a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1V2a1 1 0 0 1 1-1z"/>
      </svg>
      </button>


        </div> -->
      </div>
    </div>
  </div>
</div>
